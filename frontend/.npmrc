# react-day-picker@8 peer-requires date-fns ^2||^3 while this project uses date-fns@4.
# Only the unused shadcn ui/calendar.jsx imports it, so the conflict is cosmetic — but npm@7+
# hard-fails on it. Pinning this here keeps local installs and Vercel builds identical.
legacy-peer-deps=true
